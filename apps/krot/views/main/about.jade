extends layout

block paths
	- var content_title= 'КРОТ | О нас'

block content_style
	link(rel='stylesheet', type='text/css', href='/build/css/' + __app_name + '/about.css' + version)

block content_scripts
	script(type='text/javascript', src='/build/js/' + __app_name + '/about.js' + version)

block logo
	img(src='/stuff/' + __app_name + '/icons/logo/krot_main.png')

block content
	img.banner(src='/stuff/' + __app_name + '/backgrounds/about.jpg')
	.description!= html