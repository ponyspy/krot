extends layout

block paths
	- var content_title= 'КРОТ | О нас'

block content_style
	link(rel='stylesheet', type='text/css', href='/build/' + __app_name +  '/css/about.css' + version)

block content_scripts
	script(type='text/javascript', src='/build/' + __app_name +  '/js/about.js' + version)

block logo
	img(src='/stuff/' + __app_name + '/icons/logo/krot_main.png')

block content
	img.banner(src='/stuff/' + __app_name + '/backgrounds/about.jpg')
	.description!= html