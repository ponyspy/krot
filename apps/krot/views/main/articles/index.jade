extends layout

block paths
	- var content_title= 'krot / articles'

block content_style
	link(rel='stylesheet', type='text/css', href='/build/css/' + __app_name + '/articles.css' + version)

block content_scripts
	script(type='text/javascript', src='/build/js/' + __app_name + '/articles.js' + version)

block content
	h1 articles!